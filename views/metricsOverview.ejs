<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Metrics Overview - TreeHouseBooks</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/treehouse-brand.css">
  <style>
    .metric-card {
      border: 1px solid #dee2e6;
      border-radius: 0.5rem;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      background: white;
      box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075);
    }

    .stat-badge {
      display: inline-block;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      margin: 0.25rem;
    }

    .stat-badge.primary {
      background-color: var(--thb-accent);
      color: var(--thb-primary);
    }

    .stat-badge.success {
      background-color: rgba(107, 142, 35, 0.1);
      color: var(--thb-success);
    }

    .stat-badge.warning {
      background-color: rgba(210, 105, 30, 0.1);
      color: var(--thb-warning);
    }
  </style>
</head>
<body>
  <%- include('partials/nav') %>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <h1>Metrics Overview</h1>
        <p class="text-muted">Program metrics and performance indicators</p>
      </div>
      <a href="/dashboard" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to Dashboard
      </a>
    </div>

    <% if (metricsData && metricsData.length > 0) { %>
      <% metricsData.forEach(programData => { %>
        <% if (programData.metrics && programData.metrics.length > 0) { %>
          <div class="metric-card">
            <h3 class="mb-3">
              <i class="bi bi-calendar-event text-primary"></i>
              <%= programData.program.name %>
            </h3>

            <div class="row">
              <% programData.metrics.forEach(metric => { %>
                <div class="col-md-6 col-lg-4 mb-4">
                  <div class="border rounded p-3">
                    <h5 class="text-secondary mb-3">
                      <%= metric.definition.name %>
                      <span class="badge bg-secondary"><%= metric.definition.type %></span>
                    </h5>

                    <%# Number metrics %>
                    <% if (metric.definition.type === 'number' && metric.aggregate) { %>
                      <div class="d-flex flex-wrap gap-2 mb-3">
                        <div class="stat-badge primary">
                          <strong>Avg:</strong> <%= metric.aggregate.avg %>
                        </div>
                        <div class="stat-badge success">
                          <strong>Max:</strong> <%= metric.aggregate.max %>
                        </div>
                        <div class="stat-badge warning">
                          <strong>Min:</strong> <%= metric.aggregate.min %>
                        </div>
                      </div>
                      <small class="text-muted"><%= metric.aggregate.count %> total records</small>
                    <% } %>

                    <%# Boolean metrics %>
                    <% if (metric.definition.type === 'boolean' && metric.aggregate) { %>
                      <div class="d-flex flex-wrap gap-2 mb-3">
                        <div class="stat-badge success">
                          <strong>True:</strong> <%= metric.aggregate.trueCount %> (<%= metric.aggregate.truePercentage %>%)
                        </div>
                        <div class="stat-badge warning">
                          <strong>False:</strong> <%= metric.aggregate.falseCount %>
                        </div>
                      </div>
                      <small class="text-muted"><%= metric.aggregate.count %> total records</small>
                    <% } %>

                    <%# Recent values %>
                    <% if (metric.recentValues && metric.recentValues.length > 0) { %>
                      <details class="mt-3">
                        <summary class="text-primary" style="cursor: pointer;">
                          <i class="bi bi-clock-history"></i> Recent Values
                        </summary>
                        <ul class="list-unstyled mt-2 ms-3">
                          <% metric.recentValues.forEach(val => { %>
                            <li class="mb-1">
                              <% if (metric.definition.type === 'boolean') { %>
                                <span class="badge <%= val.value ? 'bg-success' : 'bg-secondary' %>">
                                  <%= val.value ? 'Yes' : 'No' %>
                                </span>
                              <% } else { %>
                                <span class="badge bg-primary"><%= val.value %></span>
                              <% } %>
                              <small class="text-muted">
                                <%= val.member?.firstName %> <%= val.member?.lastName %> • <%= val.date %>
                              </small>
                            </li>
                          <% }) %>
                        </ul>
                      </details>
                    <% } %>
                  </div>
                </div>
              <% }) %>
            </div>
          </div>
        <% } %>
      <% }) %>
    <% } else { %>
      <div class="alert alert-info">
        <i class="bi bi-info-circle"></i>
        No metrics have been recorded yet. Metrics are created per-program and tracked over time.
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
