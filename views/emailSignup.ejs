<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Email Signup - TreeHouseBooks</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/treehouse-brand.css">
</head>
<body>
  <%- include('partials/nav') %>

  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header text-center" style="background-color: var(--thb-primary); color: white;">
            <h4 class="mb-0"><i class="bi bi-gift"></i> Happy Birthday!</h4>
          </div>
          <div class="card-body">
            <div class="text-center mb-4">
              <i class="bi bi-person-check display-1 text-success"></i>
              <h5 class="mt-3">Welcome to adulthood, <%= member.firstName %>!</h5>
              <p class="text-muted">
                You've turned 18, so your account has been upgraded to an adult membership.
              </p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
              <div class="alert alert-danger">
                <i class="bi bi-exclamation-triangle"></i> <%= error %>
              </div>
            <% } %>

            <div class="alert alert-info">
              <i class="bi bi-envelope"></i>
              <strong>Would you like to add an email address?</strong>
              <p class="mb-0 small mt-1">
                Adding an email lets us send you updates about programs, events, and book recommendations.
              </p>
            </div>

            <form method="POST" action="/visitor-checkin/email-signup">
              <div class="mb-3">
                <label for="email" class="form-label">Email Address (optional)</label>
                <input type="email" class="form-control form-control-lg" id="email" name="email"
                       placeholder="your.email@example.com">
              </div>

              <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg">
                  <i class="bi bi-check-circle"></i> Save & Check In
                </button>
                <button type="submit" name="skipEmail" value="true" class="btn btn-outline-secondary">
                  <i class="bi bi-arrow-right"></i> Skip for Now & Check In
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
