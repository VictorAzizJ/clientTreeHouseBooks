<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Programs - TreeHouseBooks</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/css/treehouse-brand.css">
  <style>
    .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px,1fr)); gap:1rem; }
    .widget { padding:1rem; border:1px solid #dee2e6; border-radius:.5rem; background:#fff; box-shadow:0 .125rem .25rem rgba(0,0,0,.075); }
  </style>
</head>
<body>
  <%- include('partials/nav') %>
  <div class="container mt-5">
    <h1>All Programs</h1>
    <div class="d-flex mb-3">
      <a href="/programs/new" class="btn btn-success me-2">+ New Program</a>
      <a href="/dashboard" class="btn btn-secondary">← Dashboard</a>
    </div>

    <div class="grid">
      <% programs.forEach(p => { %>
        <div class="widget">
          <h5><%= p.name %></h5>
          <p><%= p.description || '—' %></p>
          <a href="/programs/<%= p._id %>" class="btn btn-primary btn-sm">View Details</a>
        </div>
      <% }) %>
      <% if (programs.length===0) { %>
        <p>No programs yet.</p>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

