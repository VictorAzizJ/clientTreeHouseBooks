<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Volunteer Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
  <div class="container mt-5">
    <% if (success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <h1 class="mb-4">Welcome, <%= user.claims ? user.claims.firstName : 'Volunteer' %>!</h1>

    <!-- Bootstrap grid layout -->
    <div class="row g-4">
      <!-- Column 1: Embedded Little Green Light Form -->
      <div class="col-md-6">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <h5 class="card-title">Check-In Form</h5>
            <iframe
            onload="window.parent.scrollTo(0,0)"
            height="1080"
            allowTransparency="true"
            allow="payment"
            frameborder="0"
            scrolling="no"
            style="width:100%;border:none"
            src="https://secure.lglforms.com/form_engine/s/ALIpQGGP1QJxOuaY5sx7Lw">
            <a href="https://secure.lglforms.com/form_engine/s/ALIpQGGP1QJxOuaY5sx7Lw">Fill out my LGL Form!</a>
          </iframe>
            
            </div>
          </div>
        </div>
      </div>

      <!-- Column 2: Placeholder widgets -->
      <div class="col-md-6">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <h5 class="card-title">Coming Soon</h5>
            <p class="text-muted">This space will soon display volunteer messages, stats, or upcoming events.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-4 text-end">
      <a href="/logout" class="btn btn-danger">Logout</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
